---
const { song } = Astro.props;
const { id, data } = song;
const { name, newRelease } = data;
---

{
  newRelease ? (
    <>
      <a
        href={`/song/${id}`}
        class="w-full h-fit max-w-64 border border-white rounded-3xl px-4 py-2 mb-8 text-white text-center"
      >
        {name}
      </a>
      <h2 class="text-white text-lg">
        ¡Nuevo lanzamiento!
      </h2>
    </>
  ) : (
    <a
      href={`/song/${id}`}
      class="w-full h-fit max-w-64 border border-white rounded-3xl px-4 py-2 text-red-600 text-center"
    >
      {name}
    </a>
  )
}
